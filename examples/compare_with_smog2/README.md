# Compare With Official SMOG2 Docker

This example runs the real-PDB panel validation on 2CI2 using official SMOG2 in
Docker as the baseline and SMOG3 as the candidate.

Docker is required for this example.

Run:

```bash
bash run.sh
```

Outputs are written to ignored `real_pdb_validation/`.
