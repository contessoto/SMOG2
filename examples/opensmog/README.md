# OpenSMOG Example

This example builds an all-atom SMOG3 model and writes OpenSMOG XML.

Run:

```bash
bash run.sh
```

The OpenSMOG flags are:

```bash
-OpenSMOG -OpenSMOGxml model.xml
```

Generated files:

- `model_os.top`
- `model_os.gro`
- `model_os.ndx`
- `model_os.contacts`
- `model.xml`
